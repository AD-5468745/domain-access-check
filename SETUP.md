# 접속점검 셋업 매뉴얼 (운영자용)

이 문서대로 **한 번만** 설정하면, 담당자들은 텔레그램 채널에서 버튼만 눌러 모든 것을 하게 됩니다.
운영자(설치하는 사람)는 이후 **구글시트만** 보면 됩니다.

- 소요 시간: 약 30분
- 준비물: 구글 계정 · GitHub 계정 · 텔레그램 · NordVPN 계정
- **비용 0원, 카드 등록 없음**

---

## 0. 큰 그림 (한 번만 읽기)

```
[텔레그램 채널]  담당자들(관리자 권한) — 버튼으로 전부 조작
      ▲ │
 알림 │ ▼ 명령
[앱스스크립트 bridge.gs = 두뇌·무료 24시간]
      ├── 구글시트  ← 데이터 저장 + 운영자 모니터링
      ├── 시간 트리거 ← 정해진 시각에 점검 요청(채널에서 시각 변경 가능)
      └── GitHub 깨우기 ──▶ [Actions + NordVPN 한국] ──▶ 한국 IP로 점검 ──▶ 결과 회신
```

**왜 이렇게 나뉘나**

- 앱스스크립트는 24시간 무료로 돌지만 **구글 미국 IP**라 한국 접속 점검을 할 수 없습니다.
- GitHub Actions는 **VPN을 붙일 수 있어** 한국 IP를 만들 수 있습니다.
- 그래서 **앱스스크립트가 두뇌, GitHub이 한국 IP 팔다리** 역할을 나눠 맡습니다.

**정기 실행(cron)을 GitHub에 두지 않은 이유** — ① 담당자가 채널에서 점검 시각을 못 바꾸게 됨 ② GitHub은 저장소가 60일간 조용하면 정기 실행을 스스로 꺼버림 ③ 무료 정기 실행은 수십 분씩 늦음. 그래서 **앱스스크립트가 매시간 확인해서** 정해진 시각에 GitHub을 깨웁니다.

**가짜 결과 방지** — 점검 직전 "지금 한국 IP 맞나"를 확인하고, 아니면 점검을 건너뛰고 경고만 보냅니다.

**폴더 파일 구성**

```
check.js                     점검 본체(시트읽기·한국IP확인·점검·결과쓰기·텔레그램)
lib/core.js                  판정·정규화·리포트 로직
lib/probe.js                 실제 접속 점검
test/check.test.mjs          로직 검증
test/bridge.sim.mjs          bridge.gs 실동작 검증(구글 API를 가짜로 만들어 그대로 실행)
.github/workflows/check.yml  NordVPN 한국 접속 + 점검 실행
apps-script/bridge.gs        ★ 두뇌 — 텔레그램 조작·시트·스케줄 전부
사용법-담당자용.md            채널 담당자에게 전달할 문서
package.json / package-lock.json
README.md · SETUP.md
```

---

## 0-1. ⚠️ 분리 원칙 — 이건 '외부 업체 제공용'입니다

운영자 본인의 도메인을 점검하는 다른 시스템이 이미 있다면, **그것과 이 시스템은 완전히 갈라놓아야 합니다.**
업체가 운영자의 다른 사업을 눈치채면 안 되고, 한쪽 사고가 다른 쪽으로 번져도 안 됩니다.

| 항목 | 어떻게 |
|---|---|
| 텔레그램 봇 | ❌ **기존 봇 재사용 금지.** 반드시 새 봇을 만드세요 — 봇 이름·프로필 사진이 업체에게 그대로 보입니다 |
| 텔레그램 채널 | ✅ 새 비공개 채널 (업체 담당자만) |
| 구글시트 | ✅ **새 스프레드시트 파일.** 기존 시트에 탭을 추가하지 마세요 |
| GitHub 저장소 | ✅ 새 Private 저장소 `domain-access-check` |
| GitHub 토큰 | ✅ 이 저장소 하나만 지정한 Fine-grained 토큰 (다른 저장소 접근 불가) |
| NordVPN 자격증명 | 🔸 같은 계정 재사용 가능 (GitHub이 순간적으로만 접속하므로 충돌 없음) |
| GitHub 계정 | 🔸 **현재 선택: 같은 계정 유지** (사용량 여유 충분). 필요해지면 나중에 분리 → 12절 |

**업체가 볼 수 있는 것은 텔레그램 채널 하나뿐입니다.** 시트·GitHub·앱스스크립트는 업체에게 알려주지도, 보여주지도 않습니다.

> 업체가 여러 곳으로 늘면, **업체마다 [시트 + 앱스스크립트 + 봇 + 채널 + 저장소] 한 세트**를 새로 만듭니다.
> 한 세트를 여러 업체가 나눠 쓰면 도메인 목록이 섞입니다.

---

## 1. 구글시트 준비

- 구글 드라이브에서 **새 스프레드시트**를 만들고 이름을 **접속점검** 으로 합니다. (이미 만들어 두었다면 그것을 씁니다)
- 파일 이름 · 탭 이름 모두 **접속점검**
- **탭을 직접 만들 필요 없습니다.** 7단계 `setupAll` 실행 시 `접속점검`·`결과`·`이력`·`시스템` 탭이 자동 생성됩니다.
- 도메인도 직접 넣을 필요 없습니다 — 담당자가 채널에서 넣습니다.

---

## 2. 텔레그램 봇 + 채널 (7분)

**2-1. 봇 만들기**

1. 텔레그램에서 **@BotFather** → `/newbot` → 이름·아이디 입력 → **토큰** 복사 = **`BOT_TOKEN`**
   ⚠️ **반드시 이 업체 전용 새 봇을 만드세요.** 기존 봇을 재사용하면 봇 이름·프로필로 운영자의 다른 사업이 노출되고,
   봇 토큰 하나가 새면 두 시스템이 함께 뚫립니다.
2. (그룹에서도 쓸 계획이면) BotFather `/setprivacy` → 봇 선택 → **Disable**. 채널만 쓸 거면 건드리지 않아도 됩니다 — 채널 관리자인 봇은 채널 글을 모두 받습니다.

**2-2. 채널 만들기**

1. 텔레그램 → 새 채널 → **비공개(Private)** 로 생성
2. 봇을 **관리자로 추가** (권한: 메시지 게시 · 메시지 편집 켜기)
3. **조작할 담당자들도 관리자로 추가** — 채널은 관리자만 글을 쓸 수 있습니다
   ⚠️ **채널 관리자는 곧 전권입니다.** 도메인·업체를 지울 수 있으니 꼭 필요한 사람만 넣고, 그만둔 사람은 바로 빼세요.
4. 채널에 아무 글이나 하나 올린 뒤, 브라우저에서
   `https://api.telegram.org/bot<봇토큰>/getUpdates` 열기 →
   `"chat":{"id":-100…}` 의 숫자가 **`CHAT_ID`** (마이너스 포함)

> **참고 — 이력에 이름을 남기려면** 채널 설정에서 **서명(Sign messages)** 을 켜세요.
> 채널 글은 원래 "누가 썼는지"가 봇에게 전달되지 않아, 서명을 끄면 `이력` 탭에 전부 `담당자` 로만 남습니다.
> (버튼을 누른 사람은 서명과 무관하게 항상 이름이 기록됩니다.)

---

## 3. NordVPN 서비스 자격증명 (3분)

VPN 앱 로그인과 **다른** 수동설정용 자격증명입니다.

1. [nordaccount.com](https://nordaccount.com) 로그인 → **NordVPN → 수동 설정(Manual setup)**
2. **서비스 자격 증명(Service credentials)**
   - 사용자 이름 = **`NORDVPN_USER`**
   - 비밀번호 = **`NORDVPN_PASS`**

---

## 4. GitHub 저장소 (5분)

1. [github.com](https://github.com) → **New repository** → 이름 `domain-access-check` → **Private** → 생성
2. 이 폴더의 **파일 전체**를 올립니다 (`node_modules` 제외).
   - 쉬운 방법: [GitHub Desktop](https://desktop.github.com) 으로 폴더째 Commit → Push
   - 웹으로: Add file → Upload files. `.github/workflows/check.yml` 처럼 폴더가 있는 파일은 Create new file 에서 **경로까지 이름칸에 그대로** 입력

---

## 5. GitHub 토큰 발급 (3분) — 앱스스크립트가 GitHub을 깨우는 열쇠

1. GitHub → 우측 상단 프로필 → **Settings → Developer settings → Personal access tokens → Fine-grained tokens** → **Generate new token**
2. **Repository access**: Only select repositories → `domain-access-check`
3. **Permissions → Repository permissions → Actions**: **Read and write**
4. 만료일(Expiration)을 정하고 **날짜를 메모**해 두세요 → 6단계 `GITHUB_TOKEN_EXPIRES` 에 넣습니다
5. 생성된 토큰 복사 = **`GITHUB_TOKEN`**

---

## 6. 앱스스크립트 = 두뇌 만들기 (10분) — 가장 중요

**6-1. 코드 붙여넣기**

1. `접속점검` 시트 → 상단 메뉴 **확장 프로그램 → Apps Script**
2. 기본 코드(`function myFunction`)를 **전부 지우고**, 이 폴더의 **`apps-script/bridge.gs`** 내용을 통째로 붙여넣기 → 저장(💾)

**6-2. 스크립트 속성 넣기**

왼쪽 **⚙️ 프로젝트 설정 → 스크립트 속성 → 속성 추가**:

| 속성 이름 | 넣을 값 |
|---|---|
| `ACCESS_TOKEN` | **길고 아무렇게나 생긴 문자열 32자 이상** (예: 브라우저 주소창에 `javascript:` 말고, 그냥 키보드를 마구 눌러 만든 긴 문자열). 짧으면 남이 맞힐 수 있습니다. **8단계 GitHub `SHEET_BRIDGE_TOKEN` 과 똑같이 씁니다.** |
| `BOT_TOKEN` | 2-1의 봇 토큰 |
| `ALLOWED_CHAT_IDS` | 2-2의 `CHAT_ID` (`-100…`). **비워두면 모든 조작이 거부됩니다** |
| `GITHUB_TOKEN` | 5단계 토큰 |
| `GITHUB_REPO` | `내계정/domain-access-check` |
| `WEBHOOK_SECRET` | (권장) 아무 문자열 — 보안 한 겹 더 |
| `GITHUB_TOKEN_EXPIRES` | (권장) 5단계 만료일 `YYYY-MM-DD` — 30일 전부터 채널에 알림 |
| `WEBAPP_URL` | (예비) 7단계 `setupAll` 이 "웹앱 URL을 찾을 수 없습니다" 오류를 내면, 6-3의 `/exec` 주소를 여기에 넣고 다시 실행 |

> `WORKFLOW_FILE`(기본 `check.yml`)·`GIT_REF`(기본 `main`)는 그대로면 넣지 않아도 됩니다.

**6-3. 웹앱으로 배포**

1. 우측 상단 **배포 → 새 배포** → ⚙️ → **웹 앱**
2. **실행 대상: 나(내 계정)** / **액세스 권한: 모든 사용자** → **배포**
3. 처음이면 **액세스 승인(Authorize)** → 본인 계정 → (경고 화면이면 "고급 → 이동") → 허용
4. 나오는 **웹 앱 URL**(`…/exec`)을 복사 = **8단계의 `SHEET_BRIDGE_URL`**

> 액세스가 '모든 사용자'여야 GitHub·텔레그램이 접속할 수 있습니다. 대신 `ACCESS_TOKEN`(+`WEBHOOK_SECRET`)으로 잠급니다.
> URL을 잃어버리면 **배포 → 배포 관리**에서 다시 볼 수 있습니다.

---

## 7. 앱스스크립트에서 `setupAll` 실행 (1분)

편집기 상단 함수 목록에서 **`setupAll`** 을 고르고 **▶ 실행**.

이 한 번으로 다음이 전부 됩니다.

- 시트 탭 4개 생성 (`접속점검` · `결과` · `이력` · `시스템`)
- 매시간 확인 트리거 설치 (점검 시각은 기본 09시·21시)
- 텔레그램 명령 수신 준비(웹훅 제거 + 1분 폴링 트리거)
- 입력창 옆 `/` 명령 메뉴 등록 (`/menu` `/check` `/list` `/help`)
- 채널 맨 위에 **사용법 안내문 고정** (봇이 관리자여야 고정까지 됩니다)

> 실행 로그에 `✅ 설치 완료` 가 보이면 성공입니다.
> 코드를 나중에 고치면 **배포 → 배포 관리 → 편집 → 새 버전**으로 재배포해야 반영됩니다.

---

## 8. GitHub 시크릿 6개 등록 (5분)

저장소 → **Settings → Secrets and variables → Actions → New repository secret** 로 6개:

| 시크릿 이름 | 넣을 값 |
|---|---|
| `SHEET_BRIDGE_URL` | 6-3의 웹앱 URL(`…/exec`) |
| `SHEET_BRIDGE_TOKEN` | 6-2의 `ACCESS_TOKEN` 과 **똑같은 값** |
| `TELEGRAM_BOT_TOKEN` | 2-1의 봇 토큰 |
| `TELEGRAM_CHAT_ID` | 2-2의 `CHAT_ID` |
| `NORDVPN_USER` | 3단계 사용자 이름 |
| `NORDVPN_PASS` | 3단계 비밀번호 |

> 이름 철자를 정확히(대소문자 포함). 값은 GitHub이 숨겨주며 코드엔 들어가지 않습니다.

---

## 9. 시험해 보기 (5분)

1. 텔레그램 채널에 **`메뉴`** 입력 → 버튼판이 나오면 **텔레그램 연결 성공**
2. **[➕ 도메인 추가]** → (업체가 없으면) 업체 이름을 보내고 → 이어서 주소 2~3개를 줄바꿈으로 보내기 → 시트 `접속점검` 탭에 들어갔는지 확인
3. **[🔍 지금 점검]** → 3~6분 뒤 채널에 결과 요약이 오면 **전체 성공** (VPN 연결에 시간이 걸립니다)
4. 구글시트 `결과`·`이력`·`시스템` 탭이 채워졌는지 확인

**GitHub 실행 로그 확인**(문제 있을 때) — 저장소 **Actions** → 최근 실행 → `접속점검 실행` 에
`[ip] 출구 국가 = KR (OK)` 와 `[done] 총 …` 이 보이면 정상.

---

## 10. 평소 운영 — 운영자가 볼 곳은 시트뿐

담당자는 채널에서 알아서 씁니다. 운영자는 **구글시트 4개 탭**만 보면 됩니다.

| 탭 | 무엇을 보나 |
|---|---|
| **시스템** | ⭐ **여기부터 보세요.** 마지막 점검 시각·결과 요약·실행 상태·마지막 오류·토큰 만료일 |
| **결과** | 최근 점검 결과 표 (업체·도메인·상태·HTTP·최종 접속주소·응답(ms)·점검시각·비고). 형식이 잘못돼 점검하지 못한 항목은 표 아래에 따로 표시 |
| **이력** | 누가 언제 무엇을 추가·삭제했나 (최근 500건) |
| **접속점검** | 현재 등록된 업체·도메인 전체 |

> 이 밖에 `_백업` 이라는 숨김 탭이 자동으로 생깁니다 — 담당자의 **되돌리기**에 쓰이는 것이니 지우지 마세요.

**정상이라면** `시스템` 탭의 `실행 상태` 가 `대기`, `마지막 결과 도착` 이 최근 시각, `마지막 오류` 가 `-` 입니다.

> ⚠️ **시트에서 직접 도메인을 고치지 마세요.** 담당자가 채널에서 같은 순간 고치면 충돌합니다.
> 또한 `접속점검` 탭 A~O열에 메모를 적으면 다음 편집 때 지워집니다(P열부터는 안전).
> 고쳐야 할 일이 있으면 채널에서 처리하거나, 담당자에게 알려주세요.
>
> 💡 **최후의 백업은 구글시트 자체의 버전 기록입니다** — 파일 → 버전 기록 → 버전 기록 보기.
> 담당자의 '되돌리기'는 직전 1회뿐이므로, 크게 잘못됐을 때는 여기서 되살리세요.

---

## 11. 문제가 생기면

| 증상 | 원인 · 해결 |
|---|---|
| 채널에서 `ㅁ`를 쳐도 무반응 | ① **1분 기다려 보기**(대기조가 자는 동안의 폴링 주기) ② 봇이 채널 **관리자**인지 ③ `ALLOWED_CHAT_IDS` 가 맞는지 ④ `pollUpdates` 를 직접 실행해 로그 확인 ⑤ 웹앱에 `{token, action:'diag'}` 로 POST 해 상태 확인(`getWebhookInfo` 로도 확인 가능) |
| 버튼을 눌러도 무반응 | 첫 조작은 최대 1분(대기조가 깨어나는 중), 그 뒤로는 1~3초입니다. 1분이 지나도 없으면 `pollUpdates` 트리거가 살아있는지 확인 → 없으면 `setupAll` 다시 실행 |
| 항상 1분씩 걸린다(빨라지지 않음) | 대기조가 안 깨어나는 것입니다. 웹앱에 `{token, action:'diag'}` POST → `대기조.마지막_오류` 확인. 흔한 원인은 `GITHUB_TOKEN` 만료 또는 `relay.yml` 이 저장소에 없음 |
| 조작이 전부 거부됨 | `ALLOWED_CHAT_IDS` 가 비어 있음(안전 기본값). 채널 `CHAT_ID` 입력 후 저장 |
| 점검을 눌렀는데 결과가 안 옴 | 25분 뒤 채널에 자동 경고. `시스템` 탭 `마지막 오류` 확인 |
| `출구 IP 국가 = … (기대: KR)` | VPN이 한국에 못 붙음 → 다시 실행. 계속되면 NordVPN 자격증명 확인 |
| `점검 결과 폐기 — 점검 도중 VPN이 끊겼습니다` | 정상 동작입니다(한국 결과가 아닌 것을 버린 것). 다시 실행하세요 |
| 채널에서 `getUpdates` 로 chat_id를 다시 못 읽음 | 웹훅이 걸려 있으면 막힙니다. `deleteWebhook` 실행 후 다시 시도(이 시스템은 웹훅을 쓰지 않으므로 평소엔 걸려 있지 않습니다) |
| `시트 브리지 read/write 실패` | `SHEET_BRIDGE_URL`·`SHEET_BRIDGE_TOKEN`(=`ACCESS_TOKEN`) 확인. 배포 액세스가 '모든 사용자'인지 |
| 자동 점검이 안 돎 | 채널 `설정`에서 일시중지 상태인지 확인. 앱스스크립트 트리거 목록에 `hourlyTick` 이 있는지(`applySchedule_` 실행) |
| GitHub 토큰 만료 | 5단계로 새로 발급 → `GITHUB_TOKEN`·`GITHUB_TOKEN_EXPIRES` 갱신 |
| 코드를 고쳤는데 반영 안 됨 | 앱스스크립트는 **배포 → 배포 관리 → 편집 → 새 버전** 으로 재배포해야 함 |

**편집기에서 직접 실행할 수 있는 점검 함수**

| 함수 | 하는 일 |
|---|---|
| `testRead` | 시트가 제대로 읽히는지 (로그 확인) |
| `deleteWebhook` | 텔레그램 연결 해제 (chat_id를 다시 읽어야 할 때) |
| `testChannel` | 채널로 시험 메시지 보내기 |
| `getWebhookInfo` | 텔레그램 연결 상태 (토큰은 가려서 출력) |
| `applySchedule_` | 스케줄 다시 설치 |
| `pollUpdates` | 채널 명령을 지금 즉시 가져와 처리(평소엔 1분마다 자동) |
| `setupCommands` | 입력창 옆 `/` 명령 메뉴 다시 등록 |
| `pinGuide` | 사용법 안내문을 다시 보내고 채널 상단에 고정 |

---

## 11-1. 대기조 — 버튼이 1~3초 만에 반응하는 이유

앱스스크립트는 트리거 최소 간격이 **1분**이라, 혼자서는 절대 1분보다 빨라질 수 없습니다.
텔레그램 웹훅은 앱스스크립트가 302(넘김)를 돌려주기 때문에 못 씁니다(2026-09-04 실측).

그래서 **담당자가 첫 조작을 한 순간에만** 깃허브에서 '대기조'(`relay.yml` → `relay.js`)를 깨웁니다.

| 상황 | 반응 |
|---|---|
| 오래 조용하다가 첫 조작 | 최대 1분 (대기조가 깨어나는 시간) |
| 그 뒤 연속 조작 | **1~3초** |
| 10분간 조용하면 | 대기조가 스스로 종료 → 다시 1분 방식 |

**중요 — 대기조는 속도만 담당합니다.** 대기조가 죽거나 깃허브가 막혀도 기능은 하나도 안 없어집니다.
하트비트가 90초간 끊기면 앱스스크립트 1분 폴링이 자동으로 되돌아옵니다.

끄고 싶으면 스크립트 속성에 `RELAY_ENABLED` = `no` 를 넣으면 됩니다(그러면 항상 1분 방식).

> 대기조는 **저장소가 공개(Public)** 라서 실행 시간이 무료입니다.
> 로그에는 건수만 남기고 메시지 내용·주소·업체명·chat_id 는 절대 찍지 않습니다.

---

## 12. 한도 (무료로 쓸 수 있는 범위)

**⚠️ 한도는 GitHub 계정 전체가 나눠 씁니다** (저장소별이 아님). 같은 계정에 다른 자동화가 돌고 있다면 합산됩니다.
공개(Public) 저장소는 무제한이라 한 푼도 차감되지 않습니다.

**현재 선택: 같은 계정 유지.** 2026-08 기준 443분/2,000분 사용이라 접속점검(월 200~400분)을 더해도 40%대입니다.

**나중에 분리할 때** — 이 시스템만 위한 **GitHub 무료 계정을 하나 더** 만들어 저장소를 옮기면 됩니다.
그 계정은 2,000분을 따로 받으므로, 운영자 본인 작업이 아무리 몰려도 업체 점검이 멈추지 않습니다.
옮기는 절차는 ① 새 계정에서 Private 저장소 생성 → ② 파일 업로드 → ③ 시크릿 6개 재입력 →
④ 새 토큰 발급 후 앱스스크립트 `GITHUB_TOKEN`·`GITHUB_REPO` 교체. 코드는 손대지 않습니다.

**분리를 검토할 신호** — 월 사용량이 1,400분을 넘거나, 업체 점검이 한도로 멈춘 적이 한 번이라도 있을 때.

| 항목 | 한도 | 우리 사용량 |
|---|---|---|
| GitHub Actions (Private·Free) | 2,000분/월 | 보통 하루 2회 × 3~6분 ≈ **월 200~400분**. VPN 재시도가 겹친 최악의 달도 1,000분 안쪽 |
| 앱스스크립트 외부 호출 | 20,000회/일 | 하루 수백 회 |
| GitHub 실행 시간 상한 | 회당 최대 30분 | 보통 3~6분, 최악 20분대 |
| 대기조(`relay.yml`) | 회당 최대 20분 | 담당자가 조작할 때만. 공개 저장소라 **요금 0** |
| 구글시트 · 텔레그램 | 무료 | — |

도메인이 수백 개로 늘거나 점검을 하루 여러 번 돌리면 그때 다시 계산하면 됩니다.

---

## 요약 (한 장)

1. 구글시트 `접속점검` 만들기
2. 텔레그램 봇 + 비공개 채널(담당자 = 관리자, 봇 = 관리자) → 토큰·chat_id
3. NordVPN 서비스 자격증명
4. GitHub 저장소(Private) + 파일 업로드 + Fine-grained 토큰
5. 앱스스크립트에 `bridge.gs` 붙여넣기 → 속성 → 웹앱 배포
6. `setupAll` 실행
7. `setupAll` 실행 → GitHub 시크릿 6개
8. 채널에서 `메뉴` → 도메인 넣고 → `점검` 눌러보기

> **저장소 기본 브랜치가 `main` 인지 확인하세요.** `master` 라면 앱스스크립트 속성에 `GIT_REF` = `master` 를 추가해야 합니다.
