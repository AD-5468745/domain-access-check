# 접속점검 — 담당자 사용법

텔레그램 채널에서 관리합니다. 구글시트나 다른 프로그램은 열 필요가 없습니다.
대부분은 **버튼**으로, 몇 가지(주소 갈아끼우기·업체 옮기기·마지막 결과 보기)는 **짧은 글**로 합니다.

---

## 처음 한 번

채널에 **`ㅁ`** 한 글자만 보내세요. 아래 버튼판이 나옵니다.
(`메뉴` · `패널` · `/menu` 도 같습니다. 채널 맨 위에 고정된 안내문에도 적혀 있습니다.)

```
🎛 접속점검 관리

┃ 업체 4곳 · 도메인 24개
┃ 점검 시각 09시 · 21시
┃ 마지막 점검 2026-08-28 21:00
┃ 총 25 · ✅21 ⚠️2 ❌1 🔀1

[🔍 지금 점검]   [📋 목록 보기]
[➕ 도메인 추가] [🗑 도메인 삭제]
[🏢 업체 관리]   [⚙️ 설정]
[↩️ 되돌리기]    [❓ 도움말]
```

이 화면 하나로 모든 조작을 합니다. **답은 항상 화면 맨 아래에 새 메시지로 옵니다.**
버튼판이 위로 밀려 사라지면 다시 `ㅁ` 을 보내세요.

> ⏱ **반응 속도**
> 한동안 조용하다가 하는 **첫 조작만 최대 1분**이 걸립니다(빠른 응답 준비 중).
> 그 뒤로 **20분 동안은 이어지는 조작이 2~4초**면 답이 옵니다.
> 20분 넘게 조용하면 다시 잠들고, 다음 첫 조작이 또 최대 1분 걸립니다.
> 바로 안 온다고 여러 번 누르지 마세요 — 눌린 만큼 답이 쌓입니다.

---

## 자주 하는 일

### 주소 추가하기

1. **[➕ 도메인 추가]** 누르기
2. 어느 업체인지 버튼으로 고르기 (없으면 **[+ 새 업체]**)
   *등록된 업체가 하나도 없으면 업체 이름부터 물어보고, 이어서 주소를 물어봅니다*
3. 주소를 채널에 그냥 보내기 — **몇 개든 한 번에**

한 줄에 하나씩이든,

```
egg-77.com
https://www.egg-88.com/promo
egg-99.com
```

옆으로 죽 나열하든 똑같이 들어갑니다.

```
egg-77.com egg-88.com egg-99.com
```

> 줄바꿈 · 띄어쓰기 · 쉼표 **아무거나** 됩니다.
> **복사한 링크를 그대로 붙여넣으세요.** `https://` · `http://` · `www.` · 뒤에 붙은 경로와 **`?code=NDTV` 같은 코드까지 한 글자도 바뀌지 않고** 그대로 등록되고, 점검도 그 주소 그대로 합니다.
> `http://` 라고 적으면 http 로만, `https://` 라고 적으면 https 로만 접속해 봅니다. 스킴을 안 적으면 https → http 순으로 찾아봅니다.
> 형식이 틀린 게 하나 섞여 있어도 **나머지는 그대로 들어가고**, 틀린 것만 따로 알려줍니다.

**더 빠른 방법 ①** — 주소만 채널에 보내면 "어느 업체에 넣을까요?" 하고 물어봅니다. 버튼만 고르면 끝.

**더 빠른 방법 ② (가장 빠름)** — 맨 윗줄에 **업체 이름**, 그 아래에 주소를 붙여넣으면 물어보지도 않고 바로 들어갑니다.

```
누드티비
egg-77.com
egg-88.com
egg-99.com
```

**더 빠른 방법 ③ — 여러 업체를 한 번에**

**[➕ 도메인 추가] → [📋 여러 업체 한 번에]** 를 누르면 업체를 고르지 않고 바로 붙여넣습니다.
업체 이름과 주소를 이어서 붙여넣기만 하면 됩니다. **없는 업체는 자동으로 만들어집니다.**

```
짱구계열
egg-77.com
egg-88.com

짱구2계열
egg-99.com

누드티비
nu-1.com
```

한 줄로 이어 써도 똑같습니다.

```
짱구계열 egg-77.com egg-88.com 짱구2계열 egg-99.com
```

> 규칙 하나뿐입니다 — **주소가 아닌 말이 나오면 거기서부터 새 업체**입니다.
> 결과에 `🏢 새 업체: …` 로 새로 만들어진 곳을 알려주고, **되돌리기 한 번이면 통째로 되돌아갑니다.**

### 주소 지우기

**한 개 지우기**

1. **[🗑 도메인 삭제]** → 업체 고르기 → 지울 주소 고르기
2. **"삭제할까요?"** 에서 **[예, 삭제]**

**여러 개 한 번에 지우기**

1. **[🗑 도메인 삭제]** → 업체 고르기 → **[🗑 여러 개 한 번에]**
2. 지울 주소를 붙여넣기 (줄바꿈·띄어쓰기 아무거나)
3. 목록을 보여주며 **"N개를 지울까요?"** → **[예, 삭제]**

글로 쓰면 더 빠릅니다.

```
삭제 a.com b.com c.com
```

> 잘못 지웠으면 바로 **[↩️ 되돌리기]** 를 누르세요. **여러 개를 한 번에 지웠어도 한 번에 되돌아옵니다.**
> 같은 주소가 두 업체에 있으면 실수로 지우지 않고 "어느 업체인지 적어주세요" 하고 멈춥니다 — `삭제 a.com 누드티비`

### 지금 바로 점검하기

**[🔍 지금 점검]** → 1~3분 뒤 결과가 채널로 옵니다.

### 결과 다시 보기

**`상태`** 라고 입력하면 마지막 점검 결과를 다시 보여줍니다.

---

### 패널 숫자 읽는 법

```
업체 7곳 · 도메인 7개              ← 지금 등록된 개수
점검 시각 09시 · 21시              ← 자동으로 도는 시각
최근 점검 2026-09-05 05:07        ← 마지막으로 점검한 시각
최근 점검결과 총 7개 모두 정상 ✅    ← 그 점검의 성적표

🔄 목록을 고친 뒤 아직 점검하지 않았습니다 — [🔍 지금 점검]
```

**`최근 점검결과` 뒤가 그 점검의 성적표**입니다.

| 이렇게 보이면 | 뜻 |
|---|---|
| `총 7개 모두 정상 ✅` | 7개 다 잘 열림 |
| `총 7 · ✅5 ⚠️1 ❌1 🔀0` | 정상 5 · 제한 1 · 이상 1 · 주소확인 0 |

> **접속에 문제가 있는 것만 알려드립니다.** 사이트가 "사람인지 확인 중" 화면을 띄우는 경우
> (사람은 잠깐 기다리거나 한 번 누르면 들어가는 그 화면)는 **정상**으로 봅니다 —
> 한국에서 진짜 막히면 답이 **아예 안 오거나** `warning.or.kr` 차단 안내로 넘어가는데,
> 그건 ❌ 로 정확히 잡습니다.
| `점검할 주소가 없어 건너뜀` | 그 시각엔 등록된 주소가 하나도 없었음 |
| `최근 점검 아직 없음` | 아직 한 번도 점검한 적 없음 |

> 목록을 고친 뒤에는 `🔄` 표시가 뜹니다 — [🔍 지금 점검]을 한 번 눌러 최신 성적표로 바꿔주세요.

---

## 결과 읽는 법

**문제가 있을 때**

```
🌐 접속점검 결과 · 2차(자동)
🕒 2026-08-28 21:00 KST

┃ 총 25개
┃ ✅ 정상 21 · ⚠️ 제한 2 · ❌ 이상 1 · 🔀 주소확인 1

⚠️ 확인 필요

┃ 〔누드티비〕
┃ ❌ egg-4841.com — 접속실패(타임아웃)
┃ ⚠️ egg-5.com — 제한응답(403)

┃ 〔파트너사〕
┃ 🔀 ya-4917.com — 주소확인(리다이렉트 감지) → ya-9002.com
```

**모두 정상일 때** (기본 설정에서는 정상이어도 매번 옵니다 — 시스템이 살아있다는 확인.
[⚙️ 설정]에서 '문제 있을 때만'으로 바꾸면 이 메시지는 오지 않습니다)

```
🌐 접속점검 결과 · 1차(자동)
🕒 2026-08-28 09:00 KST

┃ 총 24개 모두 정상 ✅
```

| 표시 | 뜻 | 어떻게 하나 |
|---|---|---|
| ✅ **정상** | 한국에서 잘 열림 | 그대로 두면 됨 |
| ⚠️ **제한** | 접속은 되는데 서버가 막거나 혼잡(403·429·503) | 대개 사이트는 살아있음. 계속 뜨면 확인 |
| ❌ **이상** | 아예 안 열림(끊김·주소없음·차단·404) | **업체에 문의** |
| 🔀 **주소확인** | 열리긴 하는데 **다른 주소로 넘어감** | 제휴 주소가 바뀐 신호. 새 주소로 교체 |

---

## 설정 바꾸기

**[⚙️ 설정]** 에서:

- **[🕘 점검 시각 바꾸기]** — 하루 최대 6번까지. 예) `9 21` → 매일 09시·21시 (한국시간)
- **[🔔 알림: 항상 → 문제만]** — 누를 때마다 바뀝니다 (현재 설정이 버튼에 적혀 있습니다)
- **[⏸ 자동 점검 일시중지] / [▶️ 자동 점검 재개]** — 자동 점검만 멈춤 (수동 점검은 계속 됨)

---

## 글로 바로 쓰기 (버튼 대신)

버튼이 빠르지만, 익숙해지면 글이 더 빠릅니다.

```
점검                      지금 점검
목록                      전체 보기
상태                      마지막 결과 다시 보기

추가 누드티비 a.com b.com    업체에 주소 추가 (몇 개든)
삭제 a.com b.com          주소 삭제 (몇 개든)
변경 a.com b.com          주소 갈아끼우기 (짝으로)
이동 a.com b.com 파트너사    다른 업체로 옮기기 (몇 개든)

업체추가 새업체             (줄바꿈으로 여러 곳)
업체삭제 누드티비            (확인 버튼이 뜹니다 · 줄바꿈으로 여러 곳)
이름변경 누드티비 누드티비2    (줄바꿈으로 한 줄에 한 짝씩)

점검시각 9 21             매일 09시·21시로
알림 문제만 / 알림 항상
일시중지 / 재개
되돌리기                  직전 변경 취소

ㅁ (또는 메뉴 · 패널)      버튼판 부르기
도움말
취소                      진행 중인 작업 중단
```

---

## 여러 개를 한 번에 — 한눈에

| 하고 싶은 것 | 이렇게 |
|---|---|
| **여러 업체 × 여러 주소 한 번에** | [➕ 도메인 추가] → **[📋 여러 업체 한 번에]** → 업체 이름 → 그 주소들 → 다음 업체… (없는 업체는 자동 생성) |
| 주소 여러 개 등록 | 업체 이름을 맨 윗줄에 쓰고 아래에 주소 붙여넣기 |
| 〃 (업체를 버튼으로 고르고 싶다) | 주소만 붙여넣기 → 업체 버튼 고르기 |
| 〃 (글로) | `추가 누드티비 a.com b.com c.com` |
| 주소 여러 개 삭제 (버튼) | [🗑 도메인 삭제] → 업체 → **[🗑 여러 개 한 번에]** |
| 주소 여러 개 삭제 (글) | `삭제 a.com b.com c.com` |
| 여러 개 다른 업체로 이동 | `이동 a.com b.com 파트너사` |
| 주소 여러 개 갈아끼우기 | `변경` 다음 줄부터 `옛주소 새주소` 한 줄에 한 짝 |
| 업체 여러 곳 추가 | `업체추가` 다음 줄부터 한 줄에 한 곳 |
| 업체 여러 곳 삭제 | `업체삭제` 다음 줄부터 한 줄에 한 곳 (확인 한 번) |
| 업체 이름 여러 개 변경 | `이름변경` 다음 줄부터 `옛이름 새이름` 한 줄에 한 짝 |

> 어떤 대량 작업이든 **결과를 한 줄씩 알려줍니다** — 들어간 것(+), 이미 있던 것(⏭), 형식이 틀린 것(⚠️).
> 그리고 **되돌리기 한 번이면 그 대량 작업 전체가 되돌아옵니다.**

---

## 이럴 땐?

| 이런 일이 | 이렇게 |
|---|---|
| 실수로 지웠다 | 바로 **`되돌리기`** (직전 작업 1회) |
| 같은 주소가 두 업체에 있어서 삭제가 안 된다 | `삭제 a.com 누드티비` 처럼 **업체를 같이** 적으세요 |
| 주소를 넣었는데 "형식이 아님"이 뜬다 | 오타이거나 점(`.`)이 빠졌는지 확인 |
| 버튼을 눌렀는데 "취소되었습니다"가 뜬다 | 5분이 지나 만료됐거나, 중간에 다른 말을 써서 취소된 것. 처음부터 다시 |
| "지난 확인 버튼입니다" | 위로 스크롤해서 **오래된** 확인 버튼을 누른 것. 엉뚱한 걸 지우지 않으려고 막습니다. 새로 시작하세요 |
| 주소가 많아 삭제 버튼에 안 보인다 | 한 번에 40개까지만 버튼으로 나옵니다. `삭제 주소` 라고 직접 쓰세요 |
| "최대 15곳"·"최대 200개"·"20자 이내" | 업체는 15곳, 업체당 주소 200개, 업체 이름 20자까지입니다 |
| 점검을 눌렀는데 결과가 안 온다 | 25분 뒤 자동으로 경고가 옵니다. 그때 다시 눌러보세요 |
| "○○님이 지금 작업 중입니다" | 다른 담당자가 절차를 진행 중입니다. **2분이 지나면 자동으로 풀립니다.** 채널 글에는 누가 썼는지가 안 남기 때문에, 입력이 섞이지 않도록 한 번에 한 사람만 진행합니다 |
| "다른 작업이 진행 중입니다" | 같은 순간에 저장이 겹친 것. 잠시 뒤 다시 |
| 버튼판이 안 보인다 | `ㅁ` 한 글자를 보내면 맨 아래에 다시 나옵니다 |
| 첫 조작만 유난히 느리다 | 정상입니다. 조용하던 상태에서 깨어나는 데 최대 1분이 걸리고, 그 뒤로는 1~3초입니다 |
| 아무 반응이 없다 | 채널 관리자 권한이 있는지 확인 (관리자만 조작 가능) |

문제가 계속되면 운영자에게 알려주세요. 운영자는 구글시트의 **`시스템`** 탭에서 무엇이 잘못됐는지 볼 수 있습니다.
